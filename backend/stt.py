import os

def transcribe_audio_if_needed(content: bytes, filename: str) -> str:
    # F1 stub — zwróć placeholder. W F2 podmień na Whisper/Azure Speech.
    return "(audio transcription placeholder) Provide a .txt transcript in F1. Filename: " + filename
